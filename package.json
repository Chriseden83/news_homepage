{
  "name": "news-homepage",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile": "sass --watch --no-source-map scss/style.scss style.css"
  },
  "devDependencies": {
    "sass": "^1.78.0",
    "vite": "^5.4.1"
  }
}
